
<nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <div class="flex-shrink-0">
                    <a href="#" class="text-2xl font-bold text-grey-800">FaceDev</a>
                  <%= link_to 'Dev Community', root_path %>
                  <% if user_signed_in? %>
                  <%= link_to 'My profile', member_path(current_user) %>

                <%= link_to 'Sign Out', destroy_user_session_path, data: {turbo_method: :delete} %>
                <% else %>
               <%= link_to 'Sign in', new_user_session_path %>
                <% end %>
                </div>
                <div class="hidden md:block">
                    <div class="flex items-center">
                        <input type="text" placeholder="Search..."
                         class="py-1 px-2 border border-gray-300 rounded-md focus:outline-none focus:border-blue-500">
                       
                    </div>
                </div>
                <div class="md:hidden">
                    <button
                        class="mobile-menu-button inline-flex items-center justify-center p-2 rounded-md text-gray-700 hover:text-gray-900 focus:outline-none focus:ring focus:border-blue-300">
                        <svg class="h-6 w-6" fill="none" stroke-linecap="round" stroke-linejoin="round"
                            stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                            <path d="M4 6h16M4 12h16m-7 6h7"></path>
                        </svg>
                    </button>
                </div>
                <div class="hidden md:flex space-x-4">
                
            </div>
            </div>
        </div>
    </nav>
